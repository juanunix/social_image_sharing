module.exports={
  'Image':require('./image'),
  'Comment':require('./comment')
};
 // If we only wanted to require a specific model somewhere in our app instead,
 // we can perform require('./models/image')
